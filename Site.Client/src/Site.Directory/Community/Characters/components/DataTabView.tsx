import React from 'react';

interface DataTabViewProps {
    // Define props here as needed
}

const DataTabView: React.FC<DataTabViewProps> = (props) => {
    return (
        <div>
            <h2>Data</h2>
            {/* Character data tables will be rendered here */}
        </div>
    );
};

export default DataTabView;
