import React from 'react';

const DataTab: React.FC = () => {
    return (
        <div>
            <h2>Data Tab</h2>
            <p>This is where users will build their own tables for character information.</p>
        </div>
    );
};

export default DataTab;
